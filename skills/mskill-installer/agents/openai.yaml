interface:
  display_name: "MSkill Installer"
  short_description: "Compile MSkills without hidden-test exposure"
  default_prompt: "Use $mskill-installer to compile an MSkill from its human-readable specification without seeing hidden tests."
