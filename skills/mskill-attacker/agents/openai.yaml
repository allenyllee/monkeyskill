interface:
  display_name: "MSkill Attacker"
  short_description: "Select allowlisted reject-canary template dimensions"
  default_prompt: "Use $mskill-attacker to select a bounded adversarial plan that trusted code will render for the mainline Tester security gate."
